# État

*Régénéré par Claude en fin de session. À coller en Chat quand la question dépend du contexte.*

**Projet actif** · P6 Sophie Bluel, étape 4 (filtre fonctionnel), non terminée. Évaluation en soutenance.
**Validés** · P1 à P5.
**Git** · tout commité et poussé, `main` à jour. Derniers commits `87ad631`, `b78f0d1`.

**Fait** · lignes 1 à 3 du pseudocode, écouteurs extraits dans `listenFilterButtons`, fichier en trois blocs, `fetch` des works dans `viewGallery`, `.catch` sur les deux fetch vérifié back-end coupé.
**Restant** · lignes 4, 5, 6. Le filtre ne filtre rien, le clic ne fait qu'un `console.log`.

**Prochaine action**

1. Trancher le nom de `viewGallery`, puis retirer les cinq `console.log` de debug.
2. Lignes 4, 5, 6 du pseudocode. Mot-clé déjà donné pour la ligne 4 : `innerHTML`.
3. Exos hors projet à froid : `async / await` en priorité, puis `console.trace()`, `event.target`, `dataset`, paramètre de callback.

**Dettes** · liste unique dans `p06/00-cadrage.md`, section `Dettes et vérifications en attente`. Non recopiée ici.

**Avant de coder** · relancer le backend (`cd Backend`, `npm start`). Arrêt avec `Ctrl + C`, jamais à la croix.
